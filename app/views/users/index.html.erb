<h2>Voici les membres du club</h2>
<br/>
<table class="table table-striped table-sm">
	<thead class='bg-info'>
		<td scope="col">Prénom</td>
		<td scope="col">Nom</td>
		<td scope="col">Email</td>
	</thead>

	<% @users.each do |u| %>
		<tr scope='row'>
			<td><%= u.first_name %></td>
			<td><%= u.name %></td>
			<td><%= u.email %></td>
		</tr>
	<% end %>

</table>